<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dark_bg_surface"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="@font/orbitron_bold"
            android:letterSpacing="0.15"
            android:text="PLAYBACK PROFILES"
            android:textColor="@color/neon_pink"
            android:textSize="13sp" />

        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton xmlns:app="http://schemas.android.com/apk/res-auto"
            android:id="@+id/fabNewProfile"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:backgroundTint="@color/neon_pink"
            android:text="New Profile"
            android:textSize="11sp"
            app:icon="@android:drawable/ic_input_add" />
    </LinearLayout>

    <!-- Description -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:lineSpacingMultiplier="1.4"
        android:text="Each profile stores independent pitch, speed, EQ, trim, and loop settings. Switch instantly without losing any configuration."
        android:textColor="@color/text_secondary"
        android:textSize="12sp" />

    <!-- Profiles List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvProfiles"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="none" />

</LinearLayout>
