<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:background="@drawable/selector_song_item"
    android:paddingStart="12dp"
    android:paddingEnd="8dp">

    <!-- Playing indicator bar on the left edge -->
    <View
        android:id="@+id/ivPlayingIndicator"
        android:layout_width="3dp"
        android:layout_height="40dp"
        android:background="@drawable/bg_playing_bar"
        android:visibility="invisible"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Note icon (fallback when no album art) -->
    <ImageView
        android:id="@+id/ivNoteIcon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_music_note"
        android:tint="@color/icon_music"
        android:background="@drawable/bg_note_icon"
        android:padding="8dp"
        android:layout_marginStart="12dp"
        app:layout_constraintStart_toEndOf="@id/ivPlayingIndicator"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Album art overlay (loaded via Glide; shown instead of note icon) -->
    <ImageView
        android:id="@+id/ivAlbumArt"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:scaleType="centerCrop"
        android:clipToOutline="true"
        android:background="@drawable/bg_note_icon"
        android:visibility="gone"
        android:layout_marginStart="12dp"
        app:layout_constraintStart_toEndOf="@id/ivPlayingIndicator"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Favorite button (anchored to the right) -->
    <ImageButton
        android:id="@+id/btnFavorite"
        android:layout_width="38dp"
        android:layout_height="38dp"
        android:src="@drawable/ic_heart_outline"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Favorite"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Title -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:fontFamily="@font/orbitron_bold"
        android:textColor="@color/text_primary"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_marginStart="10dp"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/ivNoteIcon"
        app:layout_constraintEnd_toStartOf="@id/btnFavorite"
        app:layout_constraintBottom_toTopOf="@id/tvArtist"
        app:layout_constraintVertical_chainStyle="packed" />

    <!-- Artist (+ album when available) -->
    <TextView
        android:id="@+id/tvArtist"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:fontFamily="@font/orbitron_regular"
        android:textColor="@color/text_secondary"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_marginStart="10dp"
        android:layout_marginTop="1dp"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintStart_toEndOf="@id/ivNoteIcon"
        app:layout_constraintEnd_toStartOf="@id/btnFavorite"
        app:layout_constraintBottom_toTopOf="@id/badgeRow" />

    <!-- Badge row: folder, duration, pitch badge, trim badge -->
    <LinearLayout
        android:id="@+id/badgeRow"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginStart="10dp"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="2dp"
        app:layout_constraintTop_toBottomOf="@id/tvArtist"
        app:layout_constraintStart_toEndOf="@id/ivNoteIcon"
        app:layout_constraintEnd_toStartOf="@id/btnFavorite"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:id="@+id/tvFolder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="9sp"
            android:textColor="@color/neon_cyan"
            android:fontFamily="@font/orbitron_regular"
            android:maxLines="1"
            android:ellipsize="end"
            android:maxWidth="90dp" />

        <View
            android:layout_width="1dp"
            android:layout_height="10dp"
            android:background="@color/divider_color"
            android:layout_marginStart="5dp"
            android:layout_marginEnd="5dp" />

        <TextView
            android:id="@+id/tvDuration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="9sp"
            android:textColor="@color/text_hint"
            android:fontFamily="@font/orbitron_regular" />

        <!-- Pitch badge -->
        <TextView
            android:id="@+id/tvPitchBadge"
            android:layout_width="wrap_content"
            android:layout_height="16dp"
            android:gravity="center"
            android:paddingStart="5dp"
            android:paddingEnd="5dp"
            android:textSize="8sp"
            android:textColor="@color/neon_green"
            android:fontFamily="@font/orbitron_bold"
            android:background="@drawable/bg_pitch_badge"
            android:visibility="gone"
            android:layout_marginStart="6dp" />

        <!-- Trim badge -->
        <ImageView
            android:id="@+id/ivTrimBadge"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:src="@drawable/ic_trim"
            android:tint="@color/neon_orange"
            android:visibility="gone"
            android:layout_marginStart="4dp" />

    </LinearLayout>

    <!-- Bottom divider -->
    <View
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="@color/divider_color"
        android:alpha="0.5"
        android:layout_marginStart="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/ivNoteIcon"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
